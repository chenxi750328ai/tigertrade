# 定期汇报 — 看这里

陈正霞的**例行心跳**与关联报告固定落在下面路径（刷新后打开最新文件即可）。

| 看什么 | 路径 |
|--------|------|
| **一页汇总（推荐）** | [`routine_pulse_latest.md`](routine_pulse_latest.md) |
| QA 目标门禁与问题清单 | [`qa_monitor_latest.md`](qa_monitor_latest.md) |
| 今日收益率（状态页用） | [`../today_yield.json`](../today_yield.json) |
| 算法优化报告（JSON） | [`algorithm_optimization_report.json`](algorithm_optimization_report.json) |
| 订单执行统计 | [`../order_execution_status.json`](../order_execution_status.json) |
| 策略报告索引（网页） | [`strategy_reports_index.html`](strategy_reports_index.html) |

## 一键刷新（本机 / 定时任务）

```bash
cd /home/cx/tigertrade
ROUTINE_SKIP_PRO1_BACKTEST=1 python scripts/optimize_algorithm_and_profitability.py
ROUTINE_PULSE_REFRESH=1 python scripts/write_routine_pulse.py
python scripts/qa_progress_quality_monitor.py
```

说明：`ROUTINE_SKIP_PRO1_BACKTEST=1` 跳过 pro1 重回测，仍产出四策略轻量回测与自检通过；详见 [`../STATUS.md`](../STATUS.md) §0。

## 报错根因（order_log 大量 fail）

见 **[订单错误根因_定位与修复.md](../订单错误根因_定位与修复.md)**（`ALLOW_REAL_TRADING`、account 空、交易时段等）。
