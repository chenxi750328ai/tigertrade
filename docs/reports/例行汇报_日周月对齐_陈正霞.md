# Leader 例行汇报：日计划 / 周计划 / 月计划对齐（陈正霞）

> **用途**：每日收工或例会前，先填「今日」四块，再对照「本周」「本月」目标是否推进。  
> **正本计划**：[项目计划_月度周计划.md](../项目计划_月度周计划.md)  
> **每日 7 项例行**：[例行工作清单_agent必读](../../shared_rag/best_practices/例行工作清单_agent必读.md)  
> **盈利与每日目标**：[盈利目标和风险控制.md](../盈利目标和风险控制.md)  
> **心跳快照**：[routine_pulse_latest.md](routine_pulse_latest.md)（`ROUTINE_PULSE_REFRESH=1 python scripts/write_routine_pulse.py`）

---

## 一、月度目标（当前月 · 与计划文档一致）

| 维度 | 本月主题（摘自《项目计划》） | 本月可验收结果（Leader 定义） |
|------|------------------------------|-------------------------------|
| **2026-04** | 生产就绪与监控：实盘小规模、监控告警、**文档闭环** | ① CI + DFX 门禁稳定绿；② 止损/归因可审计（`dfx_execution.jsonl`）；③ 状态页与计划表同步；④ DEMO/后台可核对订单与收益口径一致 |

> 若当前自然月与上表不一致，以 [项目计划_月度周计划.md](../项目计划_月度周计划.md) 最新「月度计划概览」为准，并在此节手动改一行或 PR 更新计划文档。

---

## 二、本周目标（与月目标对齐）

**本周周期建议**：与《项目计划》周次对齐（例：**2026-04 第 1 周 · 4/1–4/7**）。

| 本周必须推进 | 对应月目标 | 验收信号 |
|--------------|------------|----------|
| 测试与门禁不回归 | 生产就绪 | `pytest -m "not real_api"` 全绿 + `verify_design_completeness_and_dfx.py` 通过 |
| DEMO 与今日成交 | 实盘可核对 | `today_yield.json` / 老虎后台有合理解释；0 成交须执行 `zero_trades_action` |
| 设计完备性落地 | 文档闭环 | 例行清单「设计完备性」= 脚本硬门禁，非仅自检 |
| 代码与文档入库 | 监控可复现 | `main` 与远端同步；Pages/status 网页为最新 |

---

## 三、今日工作计划（每日 7 项 + 目标句）

**规则**：每项用一句话写清「**今天要做到什么程度**」，避免只写动词。

| # | 例行项 | 今日目标（可勾选） |
|---|--------|-------------------|
| 1 | CI/CD 测试 | □ `pytest tests/ -m "not real_api"` 全绿；□ `verify_design_completeness_and_dfx.py` 通过 |
| 2 | 覆盖率 | □ 有变更模块则 `--cov` 看缺口或记「无代码变更跳过」 |
| 3 | 问题解决 | □ 关闭或登记当日失败用例/Bug（含根因一行） |
| 4 | 20h DEMO | □ 已运行/已启动/已记录未跑原因（休市须注明） |
| 5 | 数据与训练 | □ 有数据或训练产出，或写明「当日跳过原因」 |
| 6 | 收益率 + 算法优化 | □ 已跑 `update_today_yield` + 心跳；□ 优化脚本或记「未跑原因」 |
| 7 | 状态页 + 推送 | □ `status.html` 已更新；□ `git push` 成功或已登记重试责任人 |

**今日对齐周目标的一句话**：  
_（例：本周门禁收口 → 今日保证 CI+DFX 绿并合并推送。）_

---

## 四、今日进展（实例 · 每次刷新后可复制到日报）

**日期**：2026-04-02（以 `routine_pulse_latest` / `today_yield.json` 刷新日为准）  
**填写人**：陈正霞（Leader）

| # | 项 | 进展（事实） |
|---|----|----------------|
| 1 | 测试 | DFX 门禁 **15 passed**；全量 `not real_api` 最近一次会话 **614 passed**（合并前须再跑） |
| 2 | 覆盖率 | 本轮以门禁与回归为主，未单独产出 cov 报告 |
| 3 | 问题 | 黑盒后台可见、OrderExecutor 单测与 `client_config`/Tiger `id` 归因已修并文档化 |
| 4 | DEMO | **须持续核对**：心跳已刷新；是否跑满 20h 以本机 `logs/demo_20h_*.log` 为准 |
| 5 | 数据/训练 | 本轮未跑多模型训练；明日若无休市应安排或写明原因 |
| 6 | 收益/优化 | **已执行** `ROUTINE_PULSE_REFRESH=1 write_routine_pulse`：**今日成交 3 笔**，已实现约 **-1420.98 USD**，浮动约 **-612.99 USD**，合计约 **-2033.97 USD**；净持仓 **1 手**；QA 问题 **8** 条 |
| 7 | 状态/推送 | 文档与 CI 变更多在本地工作区，**待整理 commit + push**；Pages 自检待推送后执行 |

**与周/月目标差距（诚实一行）**：  
月目标「生产就绪」—— 门禁与归因链已加强；**远端推送与 DEMO 满跑、胜率/收益达标**仍待闭环。  
**Git**：本地已 **`git commit`**（见 `git log -1`）；**`git push gitee` / `git push origin` 须在本机有凭证的环境执行**（当前环境无交互凭证则推送失败属正常）。

---

## 五、明日最小三件事（防漏）

1. 若今日无 push：**优先** `git status` → 分组提交 → **`bash scripts/push_until_success.sh`**（或 `git push origin main` + `git push gitee main`；需本机已配置 HTTPS 凭证或 SSH）。当前环境无凭证时推送失败属正常，**在本人终端执行直至成功**。  
2. 对照 [盈利目标和风险控制.md](../盈利目标和风险控制.md)：**胜率 / 今日收益** 是否达标；不达标则列入优化或风控排查。  
3. 打开 [routine_pulse_latest.md](routine_pulse_latest.md) 核对 **订单执行核对** 与 **QA 条数** 是否下降。

---

*维护：陈正霞；与《项目计划》变更同步修订「一、二」节。*
