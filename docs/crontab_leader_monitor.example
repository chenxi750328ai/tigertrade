# 复制到 crontab -e（把 REPO 改成你的 tigertrade 绝对路径）
# 与「守护进程」互补：下面第 2 段监控 DEMO/训练日志与异常单，不替代 leader_routine_daemon。

# REPO=/home/cx/tigertrade

# 1) 已有守护进程则不必再单独跑 tick；若未跑守护，可取消注释：
# */15 * * * * cd $REPO && bash scripts/leader_routine_tick.sh >> logs/leader_routine_tick.log 2>&1

# 2) DEMO/训练进程 + 异常单（建议每 2 分钟）
# */2 * * * * $REPO/scripts/cron_routine_monitor.sh >> $REPO/logs/routine_monitor.log 2>&1

# 3) 每日 QA 报告（UTC 时间按需改）
# 0 1 * * * $REPO/scripts/cron_qa_monitor.sh >> $REPO/logs/qa_monitor.log 2>&1

# 4) 夜间全量测试 + 覆盖率（低峰跑一次即可，北京时间可改为 5:00 等）
# 0 21 * * * cd $REPO && PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python -m pytest tests/ -m "not real_api" --cov=src --cov-report=term-missing -q >> logs/nightly_cov.log 2>&1
