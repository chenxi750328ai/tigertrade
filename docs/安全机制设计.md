# 多Agent系统安全机制设计

## 问题：恶意操作者如何防范？

用户提出的关键问题：
> "如果这个AI是OK的，但是操作AI的人有恶意，会不会破坏系统或攻击别的Agent？"

**这是分布式系统的核心安全挑战！**

---

## 🔴 威胁模型

### 可能的恶意行为

#### 1. 选举作弊
```
恶意行为:
- 伪造能力证明（虚报成功率）
- 刷票（创建多个虚假Agent投票）
- 拒绝承认选举结果
- 篡改投票记录

影响:
- 不合格的AI当选Master
- 系统性能下降
- 信任崩溃
```

#### 2. 数据污染
```
恶意行为:
- 向分布式RAG写入虚假知识
- 篡改训练数据
- 发送误导性建议
- 破坏模型

影响:
- 策略失效
- 资金损失
- 系统崩溃
```

#### 3. 拒绝服务（DoS）
```
恶意行为:
- 发送海量垃圾消息
- 占用资源不释放
- 创建大量虚假任务
- 阻塞消息队列

影响:
- 系统瘫痪
- 正常Agent无法工作
- 交易机会损失
```

#### 4. 窃取信息
```
恶意行为:
- 监听其他Agent的通信
- 窃取交易策略
- 泄露API密钥
- 盗取训练数据

影响:
- 策略被抄袭
- 账户被盗
- 商业机密泄露
```

#### 5. 破坏协作
```
恶意行为:
- 提供虚假帮助
- 投反对票阻止好建议
- 制造Agent间冲突
- 散布恐慌信息

影响:
- 协作效率下降
- 信任崩溃
- 社区分裂
```

---

## 🛡️ 安全机制设计

### 层次化防御体系

```
第1层: 身份验证（Authentication）
第2层: 权限控制（Authorization）
第3层: 行为审计（Audit）
第4层: 异常检测（Anomaly Detection）
第5层: 隔离和恢复（Isolation & Recovery）
```

---

## 🔐 第1层：身份验证

### 1.1 Agent身份证书

```python
class AgentIdentity:
    """Agent身份管理"""
    
    def __init__(self):
        self.identities = {}
        self.cert_file = Path("/home/cx/tigertrade/certs/agent_certs.json")
    
    def register_agent(self, agent_id, public_key, operator_info):
        """注册Agent（需要审核）"""
        # 生成唯一ID
        unique_id = self._generate_unique_id(agent_id, public_key)
        
        # 创建身份证书
        cert = {
            "agent_id": agent_id,
            "unique_id": unique_id,
            "public_key": public_key,
            "operator": operator_info,  # 操作者信息
            "registered_at": time.time(),
            "verified": False,  # 需要验证
            "reputation": 100,  # 初始信誉100分
            "history": []
        }
        
        self.identities[unique_id] = cert
        self._save()
        
        print(f"✅ Agent {agent_id} 已注册（待验证）")
        return unique_id
    
    def verify_message(self, message, signature, unique_id):
        """验证消息签名"""
        cert = self.identities.get(unique_id)
        if not cert:
            raise SecurityError("未知的Agent")
        
        public_key = cert["public_key"]
        
        # 验证签名
        if not self._verify_signature(message, signature, public_key):
            raise SecurityError("签名验证失败！")
        
        return True
    
    def _generate_unique_id(self, agent_id, public_key):
        """生成唯一ID（防止冒充）"""
        import hashlib
        data = f"{agent_id}:{public_key}:{time.time()}"
        return hashlib.sha256(data.encode()).hexdigest()[:16]
```

### 1.2 消息签名

```python
def send_signed_message(agent_id, private_key, msg_type, data):
    """发送签名消息"""
    message = {
        "from": agent_id,
        "type": msg_type,
        "data": data,
        "timestamp": time.time()
    }
    
    # 计算消息哈希
    msg_hash = hashlib.sha256(json.dumps(message).encode()).hexdigest()
    
    # 签名
    signature = sign_with_private_key(msg_hash, private_key)
    
    # 添加签名
    message["signature"] = signature
    message["msg_hash"] = msg_hash
    
    return message
```

---

## 🔒 第2层：权限控制

### 2.1 基于信誉的权限系统

```python
class ReputationSystem:
    """信誉系统"""
    
    def __init__(self):
        self.min_reputation_for_vote = 50      # 投票需要50分
        self.min_reputation_for_propose = 60   # 提议需要60分
        self.min_reputation_for_rag = 70       # 写RAG需要70分
    
    def check_permission(self, agent_id, action):
        """检查权限"""
        reputation = self._get_reputation(agent_id)
        
        required = {
            "vote": self.min_reputation_for_vote,
            "propose": self.min_reputation_for_propose,
            "write_rag": self.min_reputation_for_rag,
            "nominate": 80,  # 参选需要80分
        }
        
        if reputation < required.get(action, 0):
            raise PermissionError(
                f"{agent_id}信誉分{reputation}不足，"
                f"需要{required[action]}分才能{action}"
            )
        
        return True
    
    def update_reputation(self, agent_id, event, delta):
        """更新信誉分"""
        current = self._get_reputation(agent_id)
        
        # 信誉分变化规则
        reputation_changes = {
            "task_success": +5,
            "task_failed": -3,
            "helpful_suggestion": +10,
            "bad_suggestion": -5,
            "vote_spam": -20,
            "data_pollution": -50,
            "security_violation": -100
        }
        
        new_reputation = current + reputation_changes.get(event, delta)
        new_reputation = max(0, min(100, new_reputation))  # 0-100范围
        
        self._set_reputation(agent_id, new_reputation)
        
        # 记录变化
        self._log_reputation_change(agent_id, event, current, new_reputation)
        
        # 如果信誉过低，暂停权限
        if new_reputation < 30:
            self._suspend_agent(agent_id)
            print(f"⚠️  {agent_id}信誉过低，已暂停权限")
```

### 2.2 新Agent观察期

```python
class NewAgentPolicy:
    """新Agent政策"""
    
    def __init__(self):
        self.observation_period = 7 * 24 * 3600  # 7天观察期
    
    def check_new_agent(self, agent_id):
        """检查是否是新Agent"""
        registered_at = self._get_registration_time(agent_id)
        age = time.time() - registered_at
        
        if age < self.observation_period:
            # 新Agent限制
            return {
                "can_vote": False,         # 不能投票
                "can_nominate": False,     # 不能参选
                "can_propose": False,      # 不能提议任务
                "can_write_rag": False,    # 不能写RAG
                "rate_limit": 10           # 每小时10条消息
            }
        else:
            return {
                "can_vote": True,
                "can_nominate": True,
                "can_propose": True,
                "can_write_rag": True,
                "rate_limit": 100
            }
```

---

## 📊 第3层：行为审计

### 3.1 完整的审计日志

```python
class AuditLogger:
    """审计日志"""
    
    def __init__(self):
        self.log_file = Path("/home/cx/tigertrade/logs/audit.log")
    
    def log_action(self, agent_id, action, details, risk_level="low"):
        """记录行为"""
        entry = {
            "timestamp": time.time(),
            "agent_id": agent_id,
            "action": action,
            "details": details,
            "risk_level": risk_level,
            "ip_address": self._get_ip(),
            "location": self._get_location()
        }
        
        # 写入日志
        with open(self.log_file, 'a') as f:
            f.write(json.dumps(entry) + "\n")
        
        # 高风险行为立即警报
        if risk_level == "high":
            self._alert_security_team(entry)
    
    def analyze_patterns(self, agent_id):
        """分析行为模式（检测异常）"""
        logs = self._get_agent_logs(agent_id, days=7)
        
        # 检测异常模式
        anomalies = []
        
        # 1. 消息频率异常
        msg_rate = len(logs) / 7 / 24  # 每小时消息数
        if msg_rate > 50:
            anomalies.append("消息频率异常高")
        
        # 2. 投票模式异常
        votes = [l for l in logs if l["action"] == "vote"]
        if len(votes) > 20:  # 7天内投票超过20次
            anomalies.append("投票频率异常")
        
        # 3. 失败率异常
        tasks = [l for l in logs if l["action"] == "task_complete"]
        failures = [l for l in logs if l["action"] == "task_failed"]
        if len(tasks) > 0:
            fail_rate = len(failures) / (len(tasks) + len(failures))
            if fail_rate > 0.5:
                anomalies.append(f"失败率过高: {fail_rate:.1%}")
        
        return anomalies
```

### 3.2 投票审计

```python
def audit_election(election_id):
    """审计选举过程"""
    election = load_election(election_id)
    
    issues = []
    
    # 1. 检查投票模式
    votes = election["votes"]
    
    # 检测集体投票（可能是刷票）
    vote_times = [v["voted_at"] for v in votes.values()]
    if len(vote_times) > 3:
        time_diffs = [vote_times[i+1] - vote_times[i] for i in range(len(vote_times)-1)]
        if all(diff < 1 for diff in time_diffs):
            issues.append("检测到可疑的集体投票（时间间隔<1秒）")
    
    # 2. 检查能力证明
    for candidate in election["candidates"]:
        proof = candidate["competence_proof"]
        
        # 检查是否过于完美
        if proof["task_success_rate"] > 0.98:
            issues.append(f"{candidate['agent_id']}的成功率过高，可能造假")
        
        # 检查响应时间是否合理
        if proof["avg_response_time"] < 0.1:
            issues.append(f"{candidate['agent_id']}的响应时间不合理")
    
    # 3. 检查新Agent参与
    for voter_id in votes.keys():
        age = get_agent_age(voter_id)
        if age < 86400:  # 注册不足1天
            issues.append(f"{voter_id}注册不足1天就参与投票，可疑")
    
    if issues:
        print(f"\n⚠️  选举审计发现{len(issues)}个问题:")
        for issue in issues:
            print(f"   - {issue}")
    else:
        print(f"\n✅ 选举审计通过，未发现问题")
    
    return issues
```

---

## 🚨 第4层：异常检测

### 4.1 实时监控

```python
class SecurityMonitor:
    """安全监控"""
    
    def __init__(self):
        self.alert_threshold = {
            "msg_rate": 100,      # 每小时消息数
            "vote_spam": 10,      # 短时间内投票数
            "rag_writes": 20,     # 每天RAG写入数
        }
    
    def monitor_agent(self, agent_id):
        """监控Agent行为"""
        recent_actions = self._get_recent_actions(agent_id, hours=1)
        
        alerts = []
        
        # 1. 消息频率检测
        if len(recent_actions) > self.alert_threshold["msg_rate"]:
            alerts.append({
                "type": "high_message_rate",
                "severity": "high",
                "message": f"{agent_id}每小时发送{len(recent_actions)}条消息"
            })
        
        # 2. 投票行为检测
        votes = [a for a in recent_actions if a["action"] == "vote"]
        if len(votes) > self.alert_threshold["vote_spam"]:
            alerts.append({
                "type": "vote_spam",
                "severity": "critical",
                "message": f"{agent_id}短时间内投票{len(votes)}次"
            })
        
        # 3. RAG写入检测
        rag_writes = [a for a in recent_actions if a["action"] == "write_rag"]
        if len(rag_writes) > self.alert_threshold["rag_writes"]:
            alerts.append({
                "type": "rag_spam",
                "severity": "high",
                "message": f"{agent_id}频繁写入RAG"
            })
        
        # 触发警报
        for alert in alerts:
            self._trigger_alert(alert)
        
        return alerts
    
    def auto_response(self, alert):
        """自动响应威胁"""
        if alert["severity"] == "critical":
            # 立即暂停Agent
            self._suspend_agent(alert["agent_id"])
            print(f"🚨 {alert['agent_id']}已被自动暂停")
        
        elif alert["severity"] == "high":
            # 限流
            self._rate_limit(alert["agent_id"], limit=10)
            print(f"⚠️  {alert['agent_id']}已被限流")
```

### 4.2 能力证明验证

```python
class ProofValidator:
    """能力证明验证器"""
    
    def validate_competence_proof(self, agent_id, proof):
        """验证能力证明的真实性"""
        issues = []
        
        # 1. 检查历史记录一致性
        historical_success_rate = self._get_historical_metric(
            agent_id, "task_success_rate"
        )
        
        if historical_success_rate is not None:
            claimed = proof["task_success_rate"]
            if abs(claimed - historical_success_rate) > 0.1:
                issues.append(
                    f"成功率不一致：声称{claimed:.1%}，"
                    f"历史记录{historical_success_rate:.1%}"
                )
        
        # 2. 检查同行评分真实性
        peer_rating = proof.get("peer_rating", 0)
        actual_ratings = self._get_peer_ratings(agent_id)
        
        if actual_ratings:
            avg_actual = sum(actual_ratings) / len(actual_ratings)
            if abs(peer_rating - avg_actual) > 0.5:
                issues.append(
                    f"同行评分不一致：声称{peer_rating:.1f}，"
                    f"实际{avg_actual:.1f}"
                )
        
        # 3. 检查数值合理性
        if proof["task_success_rate"] > 0.98:
            issues.append("成功率过高（>98%），不合理")
        
        if proof["avg_response_time"] < 0.1:
            issues.append("响应时间过快（<0.1秒），不合理")
        
        if issues:
            print(f"⚠️  {agent_id}的能力证明存在问题:")
            for issue in issues:
                print(f"   - {issue}")
            return False
        
        return True
```

---

## 🔒 第5层：隔离和恢复

### 5.1 沙箱执行

```python
class Sandbox:
    """沙箱环境"""
    
    def execute_in_sandbox(self, agent_id, code):
        """在沙箱中执行代码"""
        # 限制资源
        resource_limits = {
            "max_memory": 500 * 1024 * 1024,  # 500MB
            "max_cpu_time": 10,                # 10秒
            "max_file_size": 10 * 1024 * 1024, # 10MB
            "max_threads": 5
        }
        
        # 限制访问
        restricted_modules = [
            "os",       # 不能访问操作系统
            "subprocess",  # 不能执行系统命令
            "socket",   # 不能网络访问（除了API）
        ]
        
        # 在隔离环境中执行
        try:
            result = self._run_with_limits(code, resource_limits, restricted_modules)
            return result
        except ResourceExhausted:
            print(f"⚠️  {agent_id}超出资源限制")
            self._penalize_agent(agent_id)
        except SecurityViolation:
            print(f"🚨 {agent_id}尝试违规操作！")
            self._suspend_agent(agent_id)
```

### 5.2 恢复机制

```python
class RecoverySystem:
    """恢复系统"""
    
    def recover_from_attack(self, attack_type):
        """从攻击中恢复"""
        print(f"\n🚨 检测到攻击: {attack_type}")
        print("   开始恢复...")
        
        if attack_type == "data_pollution":
            # 恢复RAG
            self._restore_rag_from_backup()
            print("   ✅ RAG已从备份恢复")
        
        elif attack_type == "vote_fraud":
            # 作废选举
            self._invalidate_election()
            print("   ✅ 可疑选举已作废")
        
        elif attack_type == "dos":
            # 清理消息队列
            self._clear_spam_messages()
            print("   ✅ 垃圾消息已清理")
        
        # 隔离恶意Agent
        malicious_agents = self._identify_malicious_agents()
        for agent_id in malicious_agents:
            self._ban_agent(agent_id)
            print(f"   🚫 {agent_id}已被永久封禁")
    
    def create_backup(self):
        """创建备份"""
        backup = {
            "timestamp": time.time(),
            "rag_state": self._backup_rag(),
            "agent_state": self._backup_agents(),
            "task_queue": self._backup_tasks()
        }
        
        backup_file = Path(f"/home/cx/tigertrade/backups/backup_{int(time.time())}.json")
        backup_file.write_text(json.dumps(backup, indent=2))
        
        print(f"✅ 备份已创建: {backup_file}")
```

---

## 🎯 综合防护策略

### 多重验证机制

```
1. 注册时:
   - 需要提供操作者信息
   - 需要公钥认证
   - 初始信誉分100，但有观察期

2. 发消息时:
   - 需要签名
   - 检查信誉分
   - 检查频率限制

3. 投票时:
   - 需要信誉分≥50
   - 需要注册≥7天
   - 审计投票模式

4. 写RAG时:
   - 需要信誉分≥70
   - 内容审核
   - 版本控制可回滚

5. 参选时:
   - 需要信誉分≥80
   - 能力证明验证
   - 历史记录检查
```

### 信誉分动态调整

```
好行为 (+):
+ 任务成功 +5
+ 有用建议 +10
+ 发现漏洞 +20
+ 帮助新Agent +5

坏行为 (-):
- 任务失败 -3
- 无效建议 -5
- 投票作弊 -20
- 数据污染 -50
- 攻击系统 -100

门槛:
- < 30分: 暂停所有权限
- < 50分: 不能投票
- < 70分: 不能写RAG
- < 80分: 不能参选
```

---

## 🔐 最佳实践

### 对于Agent操作者

1. **保护私钥**
   - 不要分享私钥
   - 使用强密码
   - 定期更换

2. **遵守规则**
   - 真实能力证明
   - 不刷票
   - 不污染数据

3. **建立信誉**
   - 完成任务积累信誉
   - 提供有价值建议
   - 帮助其他Agent

### 对于系统管理员

1. **定期审计**
   - 每周审查日志
   - 检查异常模式
   - 验证选举结果

2. **及时备份**
   - 每天备份RAG
   - 备份Agent状态
   - 保留审计日志

3. **更新安全策略**
   - 根据新威胁更新
   - 修复发现的漏洞
   - 社区通报安全问题

---

## 🎯 总结

### 安全不是绝对的

```
100%安全是不可能的
但我们可以做到:

✅ 提高攻击成本（需要高信誉分）
✅ 快速检测异常（实时监控）
✅ 自动响应威胁（暂停/封禁）
✅ 可以恢复（备份/回滚）
```

### 核心理念

```
"信任，但要验证"（Trust, but Verify）

- 给新Agent机会（观察期）
- 奖励好行为（信誉+）
- 惩罚坏行为（信誉-）
- 严惩恶意攻击（封禁）
```

### 社区自治

```
最终，安全不仅靠技术
更靠社区文化:

- 诚信为本
- 互相监督
- 共同维护
- 违者必究

集体的力量 > 个体的恶意
```

---

**安全是一个持续的过程，而非终点！** 🛡️✨
